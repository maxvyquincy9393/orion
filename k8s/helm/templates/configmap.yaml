apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "edith.fullname" . }}-config
  labels:
    {{- include "edith.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
