version: "3.9"
services:
  orion:
    build: .
    ports:
      - "18789:18789"
      - "8080:8080"
    volumes:
      - orion_data:/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  orion_data:
